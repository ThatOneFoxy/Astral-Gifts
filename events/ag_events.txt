namespace = ag_events

planet_event = {
    id = ag_events.1
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        every_galaxy_planet = {
            if = {
                limit = {
                    is_planet_class = pc_astral_scar
                }
                create_archaeological_site = ag_scar_ruined_planet
            }
        }
    }
}